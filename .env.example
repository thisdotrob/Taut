# Optional if running with OneCLI gateway credentials.
SLACK_BOT_TOKEN=

# Optional; defaults to ./data/taut.db
TAUT_DB_PATH=./data/taut.db

# API port. Vite dev proxy expects 8787 unless vite.config.ts is changed.
PORT=8787
